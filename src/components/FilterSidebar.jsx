import PropertySearchFilterPanel from "./PropertySearchFilterPanel";

/** @deprecated Use PropertySearchFilterPanel — kept for import compatibility */
const FilterSidebar = (props) => <PropertySearchFilterPanel {...props} showCategoryPicker />;

export default FilterSidebar;
